
// Написать функцию, которая находит сумму элементов массива.
function sumArray(arr) {
    let sum = 0;

    // Написать код

    return sum;
  }
  
console.log(sumArray([1, 2, 3, 4, 5])); // 15




// Написать функцию, которая возвращает массив из всех нечетных чисел в заданном диапазоне.
function oddNumbers(start, end) {
    let result = [];


    // Написать код
    
    return result;
  }
  
  console.log(oddNumbers(1, 10)); // [1, 3, 5, 7, 9]



// Написать функцию, которая находит наибольшее значение в массиве.

function findMax(arr) {
  let max = arr[0];

  // Написать код
  
  return max;
}

console.log(findMax([1, 5, 3, 2, 4])); // 5